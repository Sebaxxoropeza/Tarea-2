#include <stdio.h>
int main()
 {
   int N, i;
  
   // Leer N
   printf("Ingresa un número entero N: ");
   scanf("%d", &N);
  
   // Imprimir tabla desde 1 hasta 10
   for (i = 1; i <= 10; i++) {
       printf("%d * %d = %d\n", N, i, N * i);
   }
  
   return 0;
}
